let n1, n2, n3

n1= 10;
n2= 20;
n3= 30;




if (n1 < n3 && n2 < n3) {
console. log (true, n3)
                        }

    if (n1 < n3 && n3 < n2)  {
    console. log (true)
                         }

    else {
    console.log (false)
}
 if (n1 < n3 || n2 < n3) {
    console.log (true)

 }
    else {
    console. log (false)
        }

 function mensaje ()    {
    console. log ("hola hola")
                        }
 mensaje ();

    function mayor (n1, n2, n3) 
    {
        if (n1 < n3 && n3 < n2) {
            console. log (30)
                                }
        
        else {
            console.log (10)
             }
    }
    mayor (10, 20, 30)

    function suma (n1, n2) {
    console . log ( n1 + n2) 
    }
    suma( 10, 20)

mayor (10, 20, 30)

/* function suma (n1, n2) {
let resultado =( n1 + n2) 
return resultado
}
suma( 10, 20)*/ 

let  peso = 5
    function holliday (peso) {
        if (peso <= 10)    { resultado = peso * 100 
        return (resultado) }

                else if ( peso => 10 && peso >= 20) 
                        {resultado = ((peso - 10) * 50) + 1000
        return(resultado)                                 
                    } 

                else if (peso > 20) 
                {resultado = ((peso - 10) * 20) + 1500
        return (resultado)
                }

                        }

console.log (holliday(peso))
console.log (`resultado" ${holliday(peso)}, el mantenimiento es ${holliday(peso)/24}`)

peso = 35
function SC (peso) {

{resultado = ((peso * 4) + 7) / (peso + 90)}
return (resultado * 1500)

}



console.log (SC (peso))

    

